<!DOCTYPE html>
<html>
    <head>
        <% include head.ejs %>
        <title>Your Profile - <%= character.name %></title>
    </head>
    <body>
        <% include nav.ejs %>
        <div class="sub-nav list">
            <div class="nav-hl"><a href="/me/campaigns/">your campaigns</a></div>
            <div><a href="/me/transactions/">your transactions</a></div>
        </div>
        <div class="list">
            <% include campaignlist.ejs %>
        </div>
        <div class="sub-nav list">
            <% if(page > 1) { %>
                <a href="/me/campaigns/?page=<%= page - 1 %>">previous</a>
            <% } %>
            <% if(campaigns.length == 18) { %>
                <a href="/me/campaigns/?page=<%= page + 1 %>">next</a>
            <% } %>
        </div>
    <% include footer.ejs %>
    </body>
</html>