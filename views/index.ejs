<!DOCTYPE html>
<html>
    <head>
        <% include head.ejs %>
        <title>ISKstarter</title>
    </head>
    <body>
        <% include nav.ejs %>
        <a href="/campaigns/">Campaigns</a>
        <div>
            <% for(let campaigns in campaigns) { %>
            <div>
                <h4><%= campaign.name %></h4>
                <progress max="<%= campaign.data.goal %>" value="<%= campaign.wallet %>"></progress>
            </div>
            <% } %>
        </div>
        <a href="/campaigns/">More…</a>
        <% include footer.ejs %>
    </body>
</html>