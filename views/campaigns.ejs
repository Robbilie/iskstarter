<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <meta name="mobile-web-app-capable" content="yes" />
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>ISKstarter</title>
        <link rel="stylesheet" href="/static/css/custom.css" />
        <script src="/static/js/bg.js"></script>
    </head>
    <body>
        <nav>
            <% if (user.id) { %>
            <img src="https://imageserver.eveonline.com/Character/<%= user.id %>_32.jpg">
            <span><%= user.name %></span>
            <span><%= user.balance %></span>
            <a href="/logout/">Logout</a>
            <% } else { %>
            <a href="/login/">Login</a>
            <% } %>
        </nav>
        <form method="POST">
            <label>Name:</label>
            <br>
            <input required="true" type="text" name="name" />
            <br>
            <label>Description:</label>
            <br>
            <textarea required="true" name="description"></textarea>
            <br>
            <label>Goal:</label>
            <br>
            <input required="true" type="number" name="goal" />
            <br>
            <span>If the Date inputs do not offer a calendar, please enter the date in the shown scheme in UTC time. If there is a calendar widget you can enter the time in local time.</span>
            <br>
            <label>Start Date:</label>
            <br>
            <input required="true" type="datetime-local" name="start" placeholder="yyyy-mm-ddThh:mm:ss" />
            <br>
            <label>End Date:</label>
            <br>
            <input required="true" type="datetime-local" name="end" placeholder="yyyy-mm-ddThh:mm:ss" />
            <br>
            <button>Submit</button>
        </form>
    </body>
</html>






